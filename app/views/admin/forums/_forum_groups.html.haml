- include_stylesheet('admin/group')
= hidden_field_tag "forum[group_ids][]", ""

.row
  %p
    Allow access only to:
    - Group.find(:all).each do |g|
      = check_box_tag "forum[group_ids][]", g.id, @forum.has_group?(g), {:id => "forum_group_#{g.id}"}
      %label{:for => "forum_group_#{g.id}"}
        = g.name
    %br
    %span.formnote
      Leave all groups unchecked for open access.